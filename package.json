{
  "name": "family-doodle-relay",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite --host 0.0.0.0\" \"cargo run\"",
    "build": "vite build",
    "test": "npm run build && cargo test && playwright test",
    "test:claims": "playwright test --grep @claim:",
    "preview": "cargo run"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "concurrently": "9.2.1",
    "typescript": "5.9.2",
    "vite": "7.1.3"
  }
}
